version: "4"

services:
  tsa-api:
    build: .
    ports:
      - "2580:2580"
    volumes:
      - .:/app
    stdin_open: true  # Keeps the container running
    tty: true         # Allows interactive terminal
